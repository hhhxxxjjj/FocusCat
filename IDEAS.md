# Ideas Backlog

> 所有冒出来的新点子,**一律记入这里**,不在 v0.1 实现。
> 这是项目能否完成的最关键纪律(见工程文档第 9 章)。

---

## 待评估(v0.1 完成后再看)

### 2026-05-02: "高兴扑跳"反应(JUMP sprite)
- 想法:Git commit 那一刻,猫从 idle 切到 JUMP.png(3 帧)再回 idle,作为奖励反馈
- 优先级:中
- 估计工作量:0.5 天
- 备注:JUMP.png 已经在素材包里;Day 8-9 接 Git 监听后再考虑要不要加。注意:这是把"反应"做得更丰富,不是"喂食"机制本身,所以是新点子

### 2026-05-02: "快饿死"状态(HURT sprite)
- 想法:饱腹感低于 20 时,猫切到 HURT.png(4 帧),配合"嗷一声扑过去"的语义
- 优先级:中
- 估计工作量:0.5 天
- 备注:HURT.png 已经在素材包里;符合工程文档 4.2 的"😱 快饿死"格

### 2026-05-02: 阈值表统一(架构债,v0.2 第一件事)
- 现状:`src/core/hunger-system.js` 的 `getLevel` 和 `src/renderer/cat.js` 的 `levelFor` 是两份独立的阈值常量(`>=80 full / >=50 normal / >=20 hungry / else starving`)
- 问题:两套数字现在恰好一致,但以后调一边忘记同步另一边就会埋 bug。renderer 用 `data-level` 决定血条颜色,所以错位的话视觉和实际语义会脱钩
- 推荐方案:主进程通过 IPC 把 `level` 字段连同 `value` 一起推下去(`cat:hunger` 事件 payload 加上 level),renderer 不再自己算
- 优先级:中(架构债,但 v0.1 不阻塞)
- 工作量:0.5 天
- 备注:用户在 v0.1 push 前 review 时点出的隐性 bug 等位

### 2026-05-02: 死亡 + 复活机制(用户提议,我作为对手方推迟到 v0.2+)
- 想法:饱腹感 0 持续 N 时间 → DEATH 动画(9 帧)→ 用户做某事(commit?点击?)→ 复活
- 拒绝理由:
  1. 跟文档 1.3 节"❌ 它不是 — 强制锁死"冲突;跟 4.2 节"快饿死(不是死)"冲突
  2. 死亡机制改变产品调性:从"萌系陪伴"漂向"Tamagotchi 严肃养成",是另一类产品
  3. 死亡条件 / 复活机制 / 复活后状态都是独立设计题,0.5-1 天工作量
  4. v0.1 收尾期不应加新机制,该收尾打包发布
- 推迟到:v0.2,等 v0.1 用户真实反馈支撑这个改动
- 备注:DEATH.png(9 帧)已在 FULL 资源包里,素材现成

---

## 已拒绝

### 用户上传图片转像素风
- 拒绝原因:独立项目,不属于本项目范畴(见工程文档 2.5)
- 拒绝日期:2026-05-02

### 在小猫身上集成 git 操作 UI(commit / push / diff)
- 拒绝原因(三条):
  1. 不在 v0.1 范围内(见工程文档 3.1 节 5 个核心功能)
  2. 跟项目灵魂哲学冲突——把猫从"陪伴者"退化成"工具"。你"用"她,而不是"和她相处"。这是 v3 → v0.1 收敛过程中砍掉的那一类
  3. 真能用的 git UI 工作量 5-7 天,会让 v0.1 发不出来。而 IDE 已经有现成 git UI
- 拒绝日期:2026-05-02
- 备注:用户原话"为小猫做一个桌宠 ui 在 ui 里 git";我作为对手方拒绝实现
